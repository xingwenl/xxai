# Phase 2D 管理后台验收记录

## 验收结论

- 当前阶段：acceptance
- 当前结论：部分满足。平台管理、后端最小绑定读取 API、宿主工具稳定冲突错误、前端 Embed Client 管理页、宿主工具策略页、侧边栏入口和路由已实现；后端定向测试、Ruff、前端本次相关文件 ESLint/Prettier 通过。
- 暂不能完整验收：前端全量 lint、format:check 和 build 仍被既有基线问题阻塞，且尚未执行真实浏览器/数据库联调。

## 已完成能力

- 平台列表、创建、编辑、停用/启用和硬删除。
- Embed Client 列表、创建、编辑、启停、轮换密钥。
- Embed Client 创建和轮换后的 `client_secret` 只在结果对话框展示一次。
- Origin 白名单在 Embed Client 表单中维护，并提示精确 Origin 规则。
- Embed Client 可绑定/解绑 Agent 和宿主工具策略。
- 宿主工具策略列表、创建、编辑、启停。
- 宿主工具策略可绑定/解绑 Agent 和 Embed Client。
- 宿主工具最近审计轻量查看。
- 后端新增绑定读取接口，支撑前端展示当前绑定状态。

## 剩余风险

- 前端全量构建受既有 `react-hook-form` 类型基线问题影响。
- 真实后端数据库和浏览器联调需要在实现后补充。
- 尚未做 Agent 详情页聚合，本次按“少页面”原则只补两个平台级资源池页面。

## 模型用量统计增量验收

### 已完成

- 新增 `/ai/model-usage` 页面和侧边栏入口。
- 新增模型用量明细分页接口与总计、按 Agent、按 Client、按日期汇总接口。
- 查询支持平台、Agent、Embed Client 和自然日范围筛选，并由平台管理员鉴权保护。
- 明细展示时间、Agent、Client、用户/会话、请求 ID、模型和三类 token；汇总支持三个分组 Tab。
- 空数据返回零值卡片和空表格；复用已有 `model_usage_records`，不新增表或迁移。
- 后端全量测试 `162 passed, 1 skipped`，模型用量定向测试 `3 passed`，Ruff、Poetry 和 PostgreSQL 迁移状态检查通过。
- 本功能前端文件级 ESLint、Prettier 和 TypeScript 检查通过。

### 尚待人工验收

- 使用已有登录态打开 `/ai/model-usage`，确认平台、Agent、Client、日期筛选、三个汇总 Tab、分页和空状态。
- 在真实数据环境抽查页面卡片总 token 与 `model_usage_records` 明细汇总一致。
- 前端全量构建仍有既有基线错误，不属于本次模型用量功能改动。

## 人工验收记录

- 2026-07-30：用户确认采用“少页面、Agent 为中心”的 2D 方向。
- 2026-07-30：用户确认允许新增绑定读取 API 和宿主工具稳定冲突错误语义。
- 2026-07-30：用户确认平台管理支持“硬删除和停用”。
- 2026-07-30：模型用量统计自动化验证完成，等待登录态浏览器与真实数据抽查。
