# Agent 对话 Token 优化设计

本设计采用每知识库相似度阈值与 Top K 控制检索上下文，配合全局上下文预算；技能只在初始上下文注入元数据，模型通过受控工具按需加载完整技能。详细调研、范围、验收标准和实施步骤见 `docs/harness/requests/2026-08-06-agent-token-optimization/`。

第一期不引入查询改写、重排模型、自适应检索或按内容类型独立阈值，以控制延迟和系统复杂度。所有技能加载必须校验当前 Agent 的绑定关系，禁止任意路径读取。
